spring:
  cloud:
    aws:
      region:
        static: us-east-1
      sqs:
        endpoint: ${SPRING_CLOUD_AWS_SQS_ENDPOINT:http://localhost:4566}
      s3:
        endpoint: ${SPRING_CLOUD_AWS_S3_ENDPOINT:http://localhost:4566}
        listener:
          acknowledgement-mode: always